import React from "react";

/**This page its like our old /todos/index so basically our root its the folder and every file it's a page now
 * Our todosList is a promise and to avoid the error we have to ignore it.
 */

const Todos = () => {
  return (
    <div>
      <h1>Todos List</h1>
    </div>
  );
};

export default Todos;
