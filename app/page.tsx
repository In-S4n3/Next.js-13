import React from "react";

/**This is the old index inside the pages folder, when creating it, it automaticaly creates a layout file that it's more or less our old app file. */

const Home = () => {
  return <div className="text-red-500">Home</div>;
};

export default Home;
